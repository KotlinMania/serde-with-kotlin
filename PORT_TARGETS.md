# Port targets — `serde-with-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `serde_with` that codex-rs uses, and therefore the minimum subset that `serde-with-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-131] (rank `85`, tier `direct`, status `To Do`)

**Cargo package name**: `serde_with`
**Rust module name (in `use` statements)**: `serde_with`

## Summary

- **2** distinct symbols imported by codex-rs
- **4** total `use` statements
- **3** codex source files affected
- **3** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `serde_with::serde_as`

- **3 `use` site(s)** across **3 codex member crate(s)**: `codex-app-server-protocol`, `codex-execpolicy-legacy`, `codex-protocol`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `app-server-protocol/src/protocol/v2.rs:122` (`codex-app-server-protocol`) — `use serde_with::serde_as`
- `execpolicy-legacy/src/error.rs:8` (`codex-execpolicy-legacy`) — `use serde_with::serde_as`
- `protocol/src/protocol.rs:60` (`codex-protocol`) — `use serde_with::serde_as`

### `serde_with::DisplayFromStr`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-execpolicy-legacy`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `execpolicy-legacy/src/error.rs:7` (`codex-execpolicy-legacy`) — `use serde_with::DisplayFromStr`
